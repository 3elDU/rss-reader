import 'package:flutter/material.dart';

class ReadLaterPage extends StatelessWidget {
  const ReadLaterPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Read later"));
  }
}
